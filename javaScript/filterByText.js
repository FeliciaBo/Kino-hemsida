import { allMovies } from "./main";

const textSearch = () => {
const input = document.querySelector('.site-search__input');
input.addEventListener('input', () => {
    const userText = input.value;
    if (userText === )
});      
}